apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: opensre
  labels:
    app: nginx
spec:
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: health
      port: 8080
      targetPort: 8080
  type: ClusterIP
